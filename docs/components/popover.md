# Popover — Moved

This per-component doc has been consolidated into the repository `README.md` as the canonical, reviewer-focused reference.

Please edit `README.md` (root) to change the canonical quick reference for Popovers. Longer examples may still live in `examples/components/`.
